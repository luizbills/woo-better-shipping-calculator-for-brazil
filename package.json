{
    "name": "wordpress-plugin",
    "version": "1.0.0",
    "scripts": {
        "postinstall": "npm run build",
        "build": "npm run minify:js",
        "minify:js": "esbuild assets/js/cart.js --minify --outfile=assets/js/cart.min.js"
    },
    "dependencies": {
        "esbuild": "^0.14.49"
    }
}
